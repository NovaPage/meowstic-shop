# How to add a new component (shadcn)

- Run: `npx shadcn add <component-name>`
- The component will be placed under `src/components/ui`.
- Import in your page/component using `@/components/ui/<component>`.
